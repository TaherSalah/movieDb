import 'package:flutter/material.dart';

class PepoleScreen extends StatelessWidget {
  const PepoleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Pepole Screen'),
    );
  }
}
